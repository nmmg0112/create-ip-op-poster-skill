# 快速开始

适用于 Codex、Claude Code 等支持文件式 Skill 的 Agent。

## 1. 安装

Codex 用户直接发送：

```text
请使用 $skill-installer，从 https://github.com/nmmg0112/create-ip-op-poster-skill 安装根目录 Skill，并命名为 create-ip-op-poster。
```

其他 Agent 将完整 Skill 文件夹放入平台规定的 Skills 目录，并保留原有目录结构。

## 2. 先确认人物素材

开始时，Agent 应先告诉你：人物肖像素材好，整张海报的呈现才会好；案例图用于提取达人真实内容中的场景、叙事、人物关系、笑点／反转和账号记忆点，避免玩法悬浮。

至少上传人物／动物原图；最好给每位达人 1–3 张代表案例截图，然后发送：

```text
请使用 $create-ip-op-poster，先处理人物素材，不要直接做海报。
```

Agent 默认只展示横版白底组合预览；后台保留同排布透明底总图，独立抠图仅在确实需要单独移动、重新分组或处理遮挡时生成。满意后回复：

```text
人物素材通过
```

案例图不一定要放进海报，也可以在人物确认后再补。没有案例时，可换成账号主页截图、代表作链接或账号介绍；全部缺失时仍可继续，但达人玩法贴合度会受限。

## 3. 再选方向和模式

上传 Brief、案例截图、Logo 和建议。Agent 会给出 2—3 个方向并解释两种模式：

- 模式 A：整张快速生图，允许生成式重绘；
- 模式 B：先生图生成位图底图，再覆回保护素材。

直接回复：

```text
选方向 1，用模式 B
```

## 4. 确认生成卡后生成

Agent 会展示一屏生成确认卡，包含主题、玩法、人物关系、第一视觉、配色、案例／Logo 用法、模式和保真边界。完整执行 Prompt 后台保留；只有你回复 `查看完整 Prompt` 时才展开。确认无误后回复：

```text
确认生成
```

不会再生成独立排布稿。若平台没有生图能力，让它输出 Prompt／交接包，不要使用 SVG、HTML、PPT 或色块页面冒充最终海报。
