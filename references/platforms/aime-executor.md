# Aime Image2 执行合同

本文件是 Aime／生态助手中的自包含执行说明。评委或用户不需要知道它的存在，也不需要复制长启动 Prompt。

## 自动开始

当用户上传或启用本 Skill，并说“帮我安装并告诉我怎么使用”“用这个 Skill 帮我做海报”或类似自然表达时，自动完成三件事：

1. 用一句话说明流程：先确认人物，再选择内容与视觉方案，最后一次生成完整海报；
2. 解释人物肖像质量决定主视觉效果，案例截图帮助提取真实内容玩法；
3. 请用户先上传人物／动物原图，并给出可选字段：主题／Brief、喜欢的风格、文字密度、必须出现的文字。

资料不全也可以先上传人物。开场白只出现一次；已有素材时直接整理，不让用户重新填写。

## 固定执行顺序

```text
读取 Skill
  -> 自动开场
  -> 人物素材处理
  -> 展示一张横版白底组合预览
  -> 等待“人物没问题”
  -> 读取 Brief、案例和视觉偏好
  -> 给出两个内容／视觉方向
  -> 等待“选 1 生成”
  -> 建立当前 LockedPosterSpec
  -> 编译最少充分 Prompt
  -> 调用 Image2 正式生图一次
  -> 查看实际成图并 QA
  -> 用户通过后建立 PosterVersionLock
```

不得增加排布稿、完整 Prompt 或技术路线确认。人物素材处理不计入正式海报生图次数。

## 当前锁定版本

`选 1 生成` 后建立唯一的 `LockedPosterSpec`，包含：批准人物、16:9 画幅、主题背景、玩法及成员、第一视觉、人物模式、文字密度、颜色／材质／光线／景深／动势、阅读顺序、案例／Logo／固定文字、参考语法、禁用项和正式生图次数。

不得把整段聊天记录原样交给 Image2。只传当前锁定版本；删除已否决方案、过期要求、平台说明、过程解释和重复禁令。用户在生成前改变主题、玩法、风格或整体构图时，创建新版本，不能把新旧要求混在一起。

## Image2 调用

- 正式海报使用 Image2；未实际调用生图能力时不得宣布成图完成。
- 默认输出一张 16:9 横版完整招商海报；不生成竖版、排布稿、空底图、PPT 信息页或第二张重复海报。
- Prompt 优先写清具体玩法、人物关系、第一视觉、完整艺术场景、信息密度和前中后景，再写少量禁令。
- 多人海报不能全部等大排排站；人物要通过动作、视线、交叠、场景和所属玩法连接。
- 用户明确要求人脸、案例、Logo 或固定文字完全不变时，按平台真实能力执行严格保真；能力不足就说明，不能假装逐像素保护。

## 成图与修改

实际打开成图检查比例、玩法、第一视觉、信息密度、人物身份、中文、案例和空间层次。竖版、无玩法、像 PPT、画面空、错脸、漏人、重复或串脸都是失败。

用户说“通过”“这版很好”后创建 `PosterVersionLock`，保留上一版成功文件。改名字、Logo、固定中文、报价、权益、案例或单个标签时只做局部修改，不得整图重绘；平台无法局部修改时直接说明。整体风格、背景、主题或人物组合改变时，保留旧版并创建新的整体版本。

## Aime 硬失败

- 要求用户复制本文件或一段长启动词；
- 没确认人物就直接出正式海报；
- 没有具体玩法就调用 Image2；
- 把整段聊天记录或已废弃要求交给 Image2；
- 默认生成竖版或 PPT 式卡片墙；
- 正式生图超过一次但没有新的用户授权；
- 为改一个标签整图重画，导致第一版人物、构图或风格改变。
